---
id: "8.1"
title: "Blog — DB: Schema"
status: Ready
epic: 8
epic_title: "Blog"
story_type: backend
complexity: Small
story_points: 2
t_shirt: S
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["manual-review"]
created_at: "2026-04-13"
updated_at: "2026-04-13"
---

# Story 8.1 — Blog: DB Schema

## Status

Ready

## Story

**Como** desenvolvedor,
**Quero** que o banco de dados tenha as tabelas e enums necessários para o blog,
**Para que** as stories seguintes possam armazenar posts, tags e comentários.

## Contexto

Primeira story do Epic 8. Cria toda a fundação de dados: enums de status/visibilidade, tabela `posts`, tabela `tags`, tabela `post_tags` (many-to-many) e tabela `post_comments`. Nenhuma interface é alterada.

## Acceptance Criteria

### AC1 — Migration
- [ ] Migration `20260413000020_blog_schema.sql` criada em `supabase/migrations/`
- [ ] Enums criados: `post_status`, `post_visibility`, `comment_status`
- [ ] Tabelas criadas: `posts`, `tags`, `post_tags`, `post_comments`
- [ ] Índice em `posts.slug` (UNIQUE já garante índice)
- [ ] Índice em `posts.status` e `posts.visibility` para queries de listagem
- [ ] Migration aplicada via `supabase db push`

### AC2 — RLS
- [ ] RLS habilitado em todas as tabelas
- [ ] `posts`: SELECT público — `visibility='public' AND status IN ('published','scheduled') AND published_at <= NOW()`
- [ ] `posts`: SELECT autenticado — sem filtro de visibility (alunos veem tudo publicado)
- [ ] `posts`: INSERT/UPDATE/DELETE — somente admin (`auth.jwt()->>'role' = 'admin'`)
- [ ] `tags`: SELECT público — sem restrição
- [ ] `tags`: INSERT/UPDATE/DELETE — somente admin
- [ ] `post_tags`: SELECT público — sem restrição
- [ ] `post_tags`: INSERT/DELETE — somente admin
- [ ] `post_comments`: SELECT — posts publicados onde status='approved'
- [ ] `post_comments`: INSERT — usuário autenticado
- [ ] `post_comments`: UPDATE/DELETE — somente admin

### AC3 — Sem regressões
- [ ] Tabelas existentes não afetadas
- [ ] `npm test` passa (32/32)

## Escopo Técnico

### Arquivos a Criar
- `supabase/migrations/20260413000020_blog_schema.sql`

### SQL de referência
```sql
CREATE TYPE post_status     AS ENUM ('draft', 'published', 'scheduled', 'archived');
CREATE TYPE post_visibility AS ENUM ('public', 'private');
CREATE TYPE comment_status  AS ENUM ('pending', 'approved', 'rejected');

CREATE TABLE posts (
  id              uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  title           text NOT NULL,
  slug            text NOT NULL UNIQUE,
  content_json    jsonb,
  cover_url       text,
  status          post_status NOT NULL DEFAULT 'draft',
  visibility      post_visibility NOT NULL DEFAULT 'public',
  author_id       uuid REFERENCES auth.users(id) ON DELETE SET NULL,
  author_name     text,
  published_at    timestamptz,
  seo_title       text,
  seo_description text,
  created_at      timestamptz DEFAULT now(),
  updated_at      timestamptz DEFAULT now()
);
CREATE INDEX posts_status_visibility_idx ON posts(status, visibility);

CREATE TABLE tags (
  id   uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  name text NOT NULL UNIQUE,
  slug text NOT NULL UNIQUE
);

CREATE TABLE post_tags (
  post_id uuid REFERENCES posts(id) ON DELETE CASCADE,
  tag_id  uuid REFERENCES tags(id)  ON DELETE CASCADE,
  PRIMARY KEY (post_id, tag_id)
);

CREATE TABLE post_comments (
  id         uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  post_id    uuid REFERENCES posts(id) ON DELETE CASCADE,
  user_id    uuid REFERENCES auth.users(id) ON DELETE SET NULL,
  user_name  text,
  content    text NOT NULL,
  status     comment_status NOT NULL DEFAULT 'pending',
  created_at timestamptz DEFAULT now()
);
```

### NÃO fazer nesta story
- Criar páginas ou componentes (8.2–8.7)
- Criar rotas de API (8.3)

## Tarefas de Implementação

- [ ] Criar migration `20260413000020_blog_schema.sql`
- [ ] Adicionar enums e tabelas
- [ ] Adicionar índices
- [ ] Adicionar políticas RLS
- [ ] Executar `supabase db push`

## Critério de Done

- [ ] Tabelas e enums existem no banco remoto
- [ ] RLS configurado corretamente
- [ ] `npm test` passa

## Dependências

- Requer Epic 7 concluído

## File List

### Criados
-

### Modificados
-

---
*Story criada por River (SM) — 2026-04-13*
