# Evidência — Story 13.2

## Ambiente e baseline

- Commit de referência: `e4b69687a52cd0681cfc848e794cfb907d976382`
- SO: `Darwin 25.6.0 arm64`
- Runtime alvo: Node `v24.20.0`, npm `11.12.1`
- SHA-256 de `package-lock.json`: `cda43c89933ecd0b598162c4b78cb7feff23a9ccb0939b13a8fed92c11914fdc` (reproduzido com npm `11.12.1`).
- Inventário final dinâmico: 126 arquivos próprios — 87 browser/React e 39 Node/API/configuração.

Antes da remediação, o baseline tinha 38 diagnósticos ESLint, 162 diagnósticos TypeScript browser e 19 Node. A classificação foi aplicada sem supressões: imports/declarações mortas, contratos JSDoc locais, estilos React, eventos, mocks e contratos Express/Supabase/PagBank; os efeitos React foram reestruturados preservando os fluxos validados pelos testes.

## Provas de runtime e instalação

| Comando | Resultado |
|---|---|
| `npm run verify:preflight` no Node 24.20.0 | `0`; aceitou 24.20.0/24.20.1/24.21.0 e rejeitou 24.19.1/23.11.0/25.0.0/24.20.0-rc.1 |
| `npm ci` no Node 24.20.0 | `0`; 555 pacotes instalados a partir do lockfile |
| `npm ls --all` e `npm ls --all --json` | `0`; `problems: []` |
| `npm ci --ignore-scripts` no Node 25.9.0 | `1`; `EBADENGINE`, como exigido por `engine-strict=true` |

O `npm audit` informou 19 vulnerabilidades transitivas; nenhuma atualização ou `npm audit fix` foi executado, pois está fora desta Story.

## Gates finais no Node 24.20.0

| Comando | Resultado |
|---|---|
| `npm run quality:scope` | `0`; filesystem, ESLint e TypeScript idênticos e disjuntos |
| `npm run lint` | `0` |
| `npm run typecheck` | `0` |
| `npm run verify:quality-canaries` | `0`; casos negativos e fases corrigidas, fingerprint antes/depois de cada subprocesso, quatro marcadores de supressão e limpeza por identidade seguros; `SIGINT`/`SIGTERM` encerram grupos ativos antes da limpeza |
| `npm test` | `0`; 15 arquivos e 99 testes passaram |
| `npm run build` | `0`; Vite 6.4.2 |
| `git diff --check` | `0` |

O Vitest emite avisos de abort de rede do happy-dom para iframes externos durante teardown, mas retorna `0`. A descoberta ainda é parcial em 15 de 17 arquivos: os dois arquivos ainda fora da configuração provisória são `api/__tests__/admin-student-password.test.js` e `api/__tests__/profile-update-avatar.test.js`. `npm run quality:test-scope` retorna `1` com `A configuração de test.projects ainda não foi materializada pela Story 13.4.` Esse limite nominal de 15/17 pertence à Story 13.4 e não é alegado como verde.

## Revalidação pós-revisão — 2026-09-04

- Runtime temporário: Node `v24.20.0`, npm `11.12.1`; o SHA-256 atual do lockfile permanece `cda43c89933ecd0b598162c4b78cb7feff23a9ccb0939b13a8fed92c11914fdc`.
- `npm run verify:preflight`, `npm ls --all`, `npm run quality:scope`, `npm run lint`, `npm run typecheck` e `npm run verify:quality-canaries`: `0`.
- `npm test`: `0`, com 16 arquivos e 100 testes passando. Os avisos conhecidos do happy-dom em teardown de iframes externos permaneceram sem afetar o exit.
- `npm run build` e `git diff --check`: `0`; não restaram diretórios `.quality-gate-canary-*`.
- `quality:test-scope` não foi promovido: continua deliberadamente vermelho até a Story 13.4.

## Patches de revisão

- `src/components/blog/FeaturedPost.jsx`: o efeito dependente mantém `onFeaturedIdRef` atualizado para a conclusão da consulta assíncrona, sem reexecutar a consulta.
- `scripts/check-quality-scope.js`: a raiz agora rejeita extensões executáveis sem contrato, inclusive variantes de caixa; os auto-testes cobrem `.mts`, `.cts` e `.ts/.tsx/.jsx` não canônicos.
- `scripts/verify-quality-canaries.js`: cada processo de TypeScript, ESLint, escopo, lint e typecheck final recebe fingerprint antes/depois; há quatro canários de supressão construídos sem introduzir marcadores no próprio script; sinal encerra os grupos ativos antes de verificar identidade e limpar arquivos/diretórios.
- `package.json` e `package-lock.json`: o manifesto fixa `packageManager: npm@11.12.1` e o lockfile foi reproduzido com esse npm.
